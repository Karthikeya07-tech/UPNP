# UPnP discovery and DLNA/AVTransport control
async-upnp-client>=0.46.0
aiohttp>=3.9.0

# Optional: for serving local files to the renderer (same network)
# Standard library http.server is used instead to keep deps minimal
